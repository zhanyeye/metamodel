# ============================================
# 时长定义元模型
# 版本: 1.0.0
# 描述: 时长定义
# ============================================
components:
  duration:
    type: StringField
    pattern: "^\d+[dhms]$"
    help: "1d（1天），2h（2小时），30m（30分钟），10s（10秒）"